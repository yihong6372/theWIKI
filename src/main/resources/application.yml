server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/wiki?characterEncoding=utf-8&userSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: password
mybatis:
  mapper-locations: classpath:/mapper/*.xml