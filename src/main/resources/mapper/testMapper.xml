<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.geekh.wiki.mapper.testMapper">
    <select id="list" parameterType="java.lang.Integer" resultType="net.geekh.wiki.domain.Test">
        select * from test where id = #{id}
    </select>
</mapper>